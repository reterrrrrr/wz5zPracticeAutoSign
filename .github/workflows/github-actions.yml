name: auto sign
on:
  workflow_dispatch:
  schedule:
    - cron: "55 23 * * *"
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: "3.x"
      - name: Install dependencies
        run: python -m pip install httpx parsel
      - name: run python script
        env:
          ENV_DINGUSERID: ${{ secrets.ENV_DINGUSERID }}
          ENV_BARKID: ${{ secrets.ENV_BARKID }}
        run: python sign.py
